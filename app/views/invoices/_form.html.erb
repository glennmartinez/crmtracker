<%= form_for [@quote, @invoice], :html => {:class => "form-inline"}  do |f| %>
	<p>
	    <%= f.label :date %>
    	<%= f.text_field :date %>
	</p>

  	<div id="buttons">
    	<br />
    	<%= f.submit "Save", :class => "btn btn-success" %>
  	</div>
<% end %>