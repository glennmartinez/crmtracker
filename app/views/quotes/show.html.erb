<div>

<p>
  <b>Name:</b>
  <%= @quote.name %>
</p>

<p>
  <b>Status:</b>
  <%= @quote.status %>
</p>

<% @quote.labouritems.each do |items|%>

	<%= items.description %>
	<%= items.hours %>
	<%= items.contractor_name %>

<% end %>

</div>



<%= link_to 'Edit', edit_project_quote_path(@project, @quote) %> |
<%= link_to 'Back to Project', client_project_path(@client, @project) %>
