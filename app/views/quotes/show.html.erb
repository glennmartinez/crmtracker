<div>

<p>
  <b>Name:</b>
  <%= @quote.name %>
</p>

<p>
  <b>Status:</b>
  <%= @quote.status %>
</p>

<% @quote.labouritems.each do |items|%>

	<%= items.description %>
	<%= items.hours %>
	<%= items.contractor_name %>

<% end %>

</div>



<%= link_to 'Edit', edit_quote_path(@quote) %> |
<%= link_to 'Back', quotes_path %>
<%= link_to 'Create Quote', new_project_quote_path(@quote) %>
